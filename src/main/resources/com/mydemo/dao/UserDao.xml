<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.mydemo.dao.UserDao">

    <!-- 查询所有 -->
    <select id="findAll" resultType="com.mydemo.domain.User">
        <!--select id as userId,username as userName,address as userAddress,sex as userSex,birthday as userBirthday from user;-->
        select * from user;
    </select>

    <!-- 保存用户-->
    <insert id="saveUser" parameterType="com.mydemo.domain.User">
        <!-- 配置插入数据后获得插入ID的操作 -->
        <selectKey keyProperty="id" keyColumn="id" resultType="int" order="AFTER">
            select LAST_INSERT_ID();
        </selectKey>
        insert into user(username,address,sex,birthday)values(#{username},#{address},#{sex},#{birthday})
    </insert>

    <!-- 更新用户 -->
    <update id="updateUser" parameterType="com.mydemo.domain.User">
        update user set username=#{username},address=#{address},sex=#{sex},birthday=#{birthday} where id=#{id}
    </update>

    <delete id="deleteUser" parameterType="java.lang.Integer">
        delete from user where id=#{uid}
    </delete>
    
    <select id="findById" parameterType="int" resultType="com.mydemo.domain.User">
        select * from user where id=#{uid}
    </select>
    
    <select id="findByName" parameterType="String" resultType="com.mydemo.domain.User">
        select * from user where username like #{username}
    </select>

    <select id="findTotal" resultType="int">
        select count(id) from user
    </select>

    <select id="findByVo" parameterType="com.mydemo.domain.QueryVo" resultType="com.mydemo.domain.User">
        select * from user where username like #{user.username}
    </select>

    <!--<select id="findUserByCondition" resultType="com.mydemo.domain.User" parameterType="user">
        select * from user where 1=1
        <if test="username != null">
            and username = #{username}
        </if>
    </select>-->

    <select id="findUserByCondition" resultType="com.mydemo.domain.User" parameterType="user">
        select * from user where
        <where>
            <if test="username != null">
                and username = #{username}
            </if>
            <if test="sex != null">
                and sex = #{sex}
            </if>
        </where>
    </select>
    
    <select id="findUserInIds" resultType="com.mydemo.domain.User" parameterType="queryVo">
        select * from user
        <where>
            <if test="ids != null and ids.size()>0">
                <foreach collection="ids" open="and id in (" close=")" item="id" separator=",">
                    #{id}
                </foreach>
            </if>
        </where>
    </select>
</mapper>